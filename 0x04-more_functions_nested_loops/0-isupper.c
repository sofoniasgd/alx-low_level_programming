#include "main.h"

/**
 * _isupper - checks case of argument
 * @c: passed int value
 * Return: 1 if n is upper, 0 if lowercase
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
else
{
return (0);
}
}
