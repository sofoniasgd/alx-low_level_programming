#include<stdio.h>
#include<stddef.h>
#include<stdlib.h>

/**
 * argstotr - returns a pointer to a 2D array of int
 * @ac: number of arguments
 * @avL pointer to arguments
 * Return: pointer to the new strings
 */
int *argstotr(int ac, char **av)
{
char *test;

return (test);
}
