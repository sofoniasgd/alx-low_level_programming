#include<stdio.h>
#include<stddef.h>
#include<stdlib.h>

/**
 * strtow - returns a pointer to a 2D array of int
 * @str: pointer to start of string
 * Return: pointer to the split strings
 */
int **strtow(char *str)
{
char **test;

return (test);
}
