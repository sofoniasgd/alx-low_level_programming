#include<stdio.h>

/**
 * main -entry point
 *
 * Return is always 0(success)
 *
 */
int main()
{

char sentence[100] = "\"Programming is like building a multilingual puzzle";
puts(sentence);
return (0);

}
