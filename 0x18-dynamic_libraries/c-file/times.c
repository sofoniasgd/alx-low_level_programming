#include "main.h"
/**
 * times_table - prints times table
 * Return: none
 */
void single_line(int x);

void times_table(void)
{
int i, j, tmp1;
for (i = 0; i <= 9 ; i++)
{
  single_line(i);
}
}

void single_line(int x)
{
int j = x;
}
