#include "main.h"
#include<stdio.h>
#include<stddef.h>
#include<stdlib.h>

/**
 * main - allocates memory for an array of integers
 * @argc: maximum value
 * @argv: minimum value
 * Return: pointer to allocated mempry
 */

void main(int argc, char *argv[])
{
void *str;
int i, j;
i = min;
j = max;
i += j;
if (argc != 3)
{
printf("Error\n");
exit(98);
}
return (str);
}
